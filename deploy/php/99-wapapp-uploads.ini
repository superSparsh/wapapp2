; Shared PHP upload limits for WhatsApp media (image 5MB, other types 14MB).
; Copy into the web PHP pool, e.g.:
;   /etc/php/8.3/fpm/conf.d/99-wapapp-uploads.ini
; then: sudo systemctl reload php8.3-fpm
upload_max_filesize = 16M
post_max_size = 20M
max_file_uploads = 20
